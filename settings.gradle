pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenCentral()
        maven { url 'https://repo.spring.io/release' }
    }
}

rootProject.name = 'HelloWorld'

include 'kafka-framework'
include 'web-server'
include 'main-program'
include 'sorting-algorithms'